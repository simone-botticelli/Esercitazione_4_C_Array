#include <iostream>
#include <fstream>
#include <sstream>
#include "Utils.hpp" // g++ requires: "src/Utils.hpp"
// #include "ClassObj.hpp" // error if pragma once is not used in ClassObj

using namespace std;

int main()
{	string inpuFile = "vectors.txt";
	size_t n;
	unsigned int * ptr1 = nullptr
	unsigned int * ptr2 = nullptr
	
	bool ImportVectors(const string& inputfilePath,
						size_t& n,
						unsigned int*& v1,
						unsigned int*& v2)
						
	ifstream fstr(inputFilePath);
	
	
			
    return 0;
}


